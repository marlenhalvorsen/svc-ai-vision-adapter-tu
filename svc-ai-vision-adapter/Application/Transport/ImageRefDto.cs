namespace svc_ai_vision_adapter.Application.Transport
{
    /// <summary>
    /// Reference to an image stored in object storage.
    /// </summary>
    public sealed record ImageRefDto(string Url);
}
